{
  "name": "navigation-bar",
  "version": "1.0.0",
  "description": "#### Simple navbar with 3 buttons, each with dropdown functionality using only one javaScript function for all.",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "webpack",
    "gh-deploy": "git push origin :gh-pages && git subtree push --prefix dist origin gh-pages",
    "gh-deploy-init": "git push origin && git subtree push --prefix dist origin gh-pages"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/stefanpython/navigation-bar.git"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "bugs": {
    "url": "https://github.com/stefanpython/navigation-bar/issues"
  },
  "homepage": "https://github.com/stefanpython/navigation-bar#readme",
  "devDependencies": {
    "webpack-cli": "^5.0.1"
  }
}
